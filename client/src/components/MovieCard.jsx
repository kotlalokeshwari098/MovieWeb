import React from 'react'

function MovieCard(prop) {
    console.log(prop)
    // prop.map((item)=>(
    //     <div>{item.Title}</div>
    // ))
  return (
    <div>
      
    </div>
  )
}

export default MovieCard
